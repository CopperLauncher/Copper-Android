<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/bgMainDrawable">

    <!-- Guideline: left pane (pref_main) = 33%, right pane (sub-screen) = 67% —
         same split as the main menu's two-pane layout, for visual consistency. -->
    <androidx.constraintlayout.widget.Guideline
        android:id="@+id/settings_pane_guideline"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:orientation="vertical"
        app:layout_constraintGuide_percent="0.33" />

    <!-- ══════════════ LEFT PANE — pref_main ══════════════ -->
    <FrameLayout
        android:id="@+id/settings_left_pane_container"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@id/settings_pane_divider" />

    <!-- Thin vertical divider -->
    <View
        android:id="@+id/settings_pane_divider"
        android:layout_width="@dimen/padding_tiny"
        android:layout_height="0dp"
        android:background="?attr/colorDivider"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="@id/settings_pane_guideline"
        app:layout_constraintEnd_toStartOf="@id/settings_pane_guideline" />

    <!-- ══════════════ RIGHT PANE — empty until a pref_main item is tapped ══════════════ -->
    <FrameLayout
        android:id="@+id/settings_right_pane_container"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginStart="6dp"
        android:layout_marginTop="6dp"
        android:layout_marginEnd="6dp"
        android:background="@drawable/bg_right_pane"
        android:clipToOutline="true"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toEndOf="@id/settings_pane_divider"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
